# OPEN-DECISIONS 悬而未决登记册

> 项目：状态承载量自测工具 v0.1
> 规则：只追加 + 就地关闭（OPEN → RESOLVED 并补 Resolution 字段），禁止整篇重写
> 每次 Phase 开始时自动复现本清单到工作上下文最前面

**当前：5 未决 / 4 已决**

| Date | Source | Open Item | Related Constraints | Current Leaning | Blocked By | Resolves When | Status |
|------|--------|-----------|---------------------|-----------------|------------|---------------|--------|
| 2026-09-09 | Phase 1 PM+架构 | **IRB / 伦理审查是否必需**：匿名采集人类受试者数据用于学位研究 | MUST 人文艺术学院具体要求未知，可能卡死上线时间 | 先把"工具功能"与"数据采集"解耦为运行时开关 `collection_enabled`，未获批准即上线 **Phase 0 纯本地版**（零采集、零数据、零风险，工具 100% 可用），批准后开开关即可 | 洪兄向学院/导师问清书面口径 | 得到答复后 | **OPEN** |
| 2026-09-09 | Phase 1 架构 | **`lucide-react` 版本号待核实**（架构文档记 `^1.37.0`） | 版本锚定防幻觉 API，写错会导致安装失败 | 已派架构师用 `npm view` 实测，以真实版本号回填 Spec | `npm view` 执行后 | 架构师回传后 | **OPEN** |
| 2026-09-09 | 独立事项（非本项目） | **Lighthouse `lhins-78odvjjb` 2026-09-15 到期**（已关自动续费） | 本项目不依赖它；但 imgbed / new-api / 个人主页会停机，**实例释放后数据不可恢复** | 不为本项目续费；若三个服务仍有价值，**9-15 前必须做整机快照** | 洪兄决策是否续费 | 9-15 前 | **OPEN · 有硬期限** |
| 2026-09-10 | Phase 1.5 设计师→架构 | **openapi `Strata.S2` / `S3` 缺 enum**：只有 `type: string` + example（`gt3y` / `daily`），无取值约束 | 契约不收紧则脏数据直接入库，EFA/CFA 阶段才发现就晚了；且 example `gt3y` 暗示的三档切法与设计师建议的四档不一致 | 补 enum（只收紧取值、不改结构）：S2 四档 `lt6m/6m_2y/2y_5y/gt5y`；S3 五档 `daily/weekly_multi/weekly_once/monthly/rarer` | 架构师裁决并回填 | 架构师回传后 | **OPEN · blocking** |
| 2026-09-10 | Phase 1.5 设计师 | **样本漏斗远超预期：约 520 次"进入第 0 步"才换 200 份入库样本** | 端到端完成率约 49%（结果前置后）；入库是显式 opt-in 且默认不入库（预估 78%）→ `200 ÷ 49% ÷ 78% ≈ 524` | 投放策略必须按 520 次进入量来规划，不是按 200；现有"一篇文章引流"的假设可能不够 | 洪兄决定投放渠道与频次 | Spec 定稿前 | **OPEN** |

---

## 已决项

| Date | Source | Open Item | Current Leaning | Resolution | Status |
|------|--------|-----------|-----------------|------------|--------|
| 2026-09-09 | Phase 1 PM | 结果页是否用 AI 生成行动建议 | 不用 | 用黑箱解释一个宣称"可解释"的量表会自毁方法论立场 → 改为**静态逐维度模板**，每条解释对应一个具体题项得分。三方一致采纳，写入 Spec。 | **RESOLVED** |
| 2026-09-10 | Phase 1 架构 | 部署目标：广州 Lighthouse 还是境外轻量 | 境外轻量 | **项目总监裁决：走中国香港/新加坡轻量。** 理由：① 架构已与部署解耦，切换只改 DNS，决策可反悔、成本极低；② 无任何证据表明手上有备案域名，而备案本身要 1–20 工作日 + 固定 IP，用户的硬时间是 10 月选导师和论文；③ 用户在珠海，中国香港地域跨境延迟最低。将来取得备案域名可随时迁回广州。 | **RESOLVED** |
| 2026-09-10 | Phase 1 三方不一致 | UIUX 漏设计 S1–S4 但 API 契约列为必填 | 中场激励方案 | **项目总监裁决：不动 API 契约，只改 UX。** 保 10 步核心路径（第 0 步 + 9 题），第 9 题答完**立即出结果预览**（只给档位 + 三因子雏形，不给完整解释），再进 S1–S3 三步（S4 可跳过），最后单次提交。设计师已撤回原 13 步顺序方案并执行，UIUX §4.3–4.5（1197 → 1612 行）。她补充的关键修正：背景段进度条需视觉降级（答到第 9 题时右侧应是"几乎看不见的三段"而非"刺眼的还差三段"），否则结果前置的收益打折。 | **RESOLVED** |
| 2026-09-10 | Phase 1 PM | 题单锁 8 题还是 9 题 | 锁 9 题 | **裁决：锁 9 题**（A3+B3+C3 三因子，补 C3 支付意愿刚性）。PM 指出 C 因子仅 2 题时 Cronbach's α 偏低；设计师 UIUX 已按 9 题设计；架构师 openapi enum 含 C3 且预留 `schema_version=1` 退回分支。**上线前定死，上线后改题须递增 `schema_version` 且新旧样本不能混跑 EFA。** | **RESOLVED** |
